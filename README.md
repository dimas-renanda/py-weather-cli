# py-weather-cli 🌦️

Python CLI that fetches weather from wttr.in (no API key needed).

## Usage
```bash
python weather.py London
python weather.py "New York"
python weather.py  # uses IP location
```
